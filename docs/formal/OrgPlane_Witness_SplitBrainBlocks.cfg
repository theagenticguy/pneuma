\* Non-vacuity witness 3 of 4 for OrgPlane.
\*
\* TLC is EXPECTED to violate SplitBrainNeverBlocks. The counterexample is the witness
\* that GateOpen's split-brain conjunct is load-bearing: a task reviews fully
\* approved, with no unresolved conflict row, held shut by a CONFIRMED split-brain
\* alone. Without this the conjunct could be dead weight.
SPECIFICATION Spec

CONSTANTS
    Runs         = {r1, r2}
    Branches     = {b1, b2}
    Decisions    = {d1, d2}
    Reviewers    = {v1, v2}
    MaxConflicts = 1
    MaxRework    = 1
    MaxBlocks    = 1
    MaxWaits     = 1

INVARIANT SplitBrainNeverBlocks
