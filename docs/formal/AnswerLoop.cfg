\* The SHIPPED design: the restart chain in team/core.py `_answer_loop`.
\*
\* K = 3 reviewing hooks, caps explored over 1..2 per hook (Init picks the cap
\* vector nondeterministically, so all 8 vectors are checked).
SPECIFICATION Spec

CONSTANTS
    K               = 3
    MaxCap          = 2
    RestartOnRevise = TRUE

INVARIANT TypeOK
INVARIANT NoUnreviewedShip
INVARIANT BudgetBounded
INVARIANT BoundedRevisions
INVARIANT CapRecordedOnlyWhenExhausted

PROPERTY BudgetMonotone
