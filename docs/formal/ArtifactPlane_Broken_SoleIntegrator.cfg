\* Guard-must-fire for SoleIntegrator: the rejected design where every member holds
\* a commit tool and fast-forwards main itself (docs/design/artifacts.md).
\*
\* TLC is EXPECTED to report SoleIntegrator violated. Exactly one defect is on, so
\* the violation is attributable to one cause.
SPECIFICATION Spec

CONSTANTS
    Members = {m1, m2}
    Lead    = lead
    Origin  = origin
    NoOne   = noone
    MaxRev  = 5
    MemberCommits = TRUE
    ForceCommit   = FALSE
    AutoMerge     = FALSE
    RewriteAuthor = FALSE

INVARIANT SoleIntegrator
