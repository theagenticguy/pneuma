\* Non-vacuity witness 3 of 3 for ArtifactPlane.
\*
\* TLC is EXPECTED to violate NoOverlapEverRefused. The counterexample is the
\* witness that OverlapNeverMerges is not passing because overlap never happens:
\* an overlapping proposal is reached, offered to the lead, and refused into a
\* conflict row.
SPECIFICATION Spec

CONSTANTS
    Members = {m1, m2}
    Lead    = lead
    Origin  = origin
    NoOne   = noone
    MaxRev  = 5

INVARIANT NoOverlapEverRefused
