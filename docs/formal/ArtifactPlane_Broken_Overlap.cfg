\* Guard-must-fire for OverlapNeverMerges: the rejected "auto-merge everything"
\* design, where an overlap is resolved by rule and one author's edit is deleted by
\* a coin toss (docs/design/artifacts.md).
\*
\* TLC is EXPECTED to report OverlapNeverMerges violated.
SPECIFICATION Spec

CONSTANTS
    Members = {m1, m2}
    Lead    = lead
    Origin  = origin
    NoOne   = noone
    MaxRev  = 5
    MemberCommits = FALSE
    ForceCommit   = FALSE
    AutoMerge     = TRUE
    RewriteAuthor = FALSE

INVARIANT OverlapNeverMerges
