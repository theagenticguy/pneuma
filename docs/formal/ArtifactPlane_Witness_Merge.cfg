\* Non-vacuity witness 2 of 3 for ArtifactPlane.
\*
\* TLC is EXPECTED to violate NoMergeEverLands. The counterexample is the witness
\* that NoLostWrite's `r \in Ancestry` disjunct is load-bearing through the SECOND
\* parent edge: a merge revision reaches main carrying `mergedFrom = r`, which is
\* the edge ArtifactStore._reachable exists to walk.
SPECIFICATION Spec

CONSTANTS
    Members = {m1, m2}
    Lead    = lead
    Origin  = origin
    NoOne   = noone
    MaxRev  = 5

INVARIANT NoMergeEverLands
