\* Non-vacuity witness 4 of 4 for OrgPlane.
\*
\* TLC is EXPECTED to violate ConflictsNeverBlock. The counterexample is the witness
\* that GateOpen's conflict conjunct is load-bearing on its own: an accepted, fully
\* reviewed, split-brain-free task held shut by an unresolved `conflicts` row.
SPECIFICATION Spec

CONSTANTS
    Runs         = {r1, r2}
    Branches     = {b1, b2}
    Decisions    = {d1, d2}
    Reviewers    = {v1, v2}
    MaxConflicts = 1
    MaxRework    = 1
    MaxBlocks    = 1
    MaxWaits     = 1

INVARIANT ConflictsNeverBlock
