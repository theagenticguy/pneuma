\* Non-vacuity witness 1 of 4 for OrgPlane.
\*
\* TLC is EXPECTED to violate NeverCompletes. The counterexample is the happy path
\* -- draft through to completed with a clean content plane -- which is what makes
\* GateSoundness a claim about a reachable step rather than about a state nothing
\* enters.
SPECIFICATION Spec

CONSTANTS
    Runs         = {r1, r2}
    Branches     = {b1, b2}
    Decisions    = {d1, d2}
    Reviewers    = {v1, v2}
    MaxConflicts = 1
    MaxRework    = 1
    MaxBlocks    = 1
    MaxWaits     = 1

INVARIANT NeverCompletes
